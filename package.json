{
  "name": "test-grpc-app",
  "version": "1.0.0",
  "description": "A repo to conduct an experiment for the implementation of gRPC with NodeJS.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "checkHealth" : "node ./scripts/checkHealthScript"
  },
  "author": "Asyraf J",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.29.0",
    "eslint-config-google": "^0.14.0",
    "nodemon": "^2.0.8"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.3.4",
    "@grpc/proto-loader": "^0.6.4"
  }
}
